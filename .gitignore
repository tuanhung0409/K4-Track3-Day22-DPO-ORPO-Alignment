# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
# NOTE: notebooks/*.ipynb IS committed for submission (with output cells preserved
# for grading). The Jupytext `.py` files are the source-of-truth template;
# `.ipynb` is the student's executed deliverable.

# Lab weight files (large — do not commit; use HF Hub for Option B)
*.safetensors
*.gguf
*.bin
gguf/
adapters/merged-fp16/
runs/
wandb/

# Allow adapter configs, tokenizer, metrics, screenshots, eval data
# (these are required for submission and are small)
# adapters/sft-mini/  <-- now committed (adapter_config.json etc.)
# adapters/dpo/       <-- now committed (adapter_config.json, dpo_metrics.json etc.)
# data/pref/          <-- now committed (train.parquet, eval.parquet)

# HuggingFace cache (Colab + local — model weights are huge)
.cache/
hf_cache/

# Env / secrets
.env
.env.local
*.token

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
